export function createScript({ term }: { term: string }) {
  return `javascript:(async()=>{const api='/jwapp/sys/wdkb/modules/xskcb/cxxszhxqkb.do';const term=${JSON.stringify(term)};console.log('[ClassTrack 课表导出] 开始请求',{api,term});try{const response=await fetch(api,{method:'POST',credentials:'include',headers:{'Content-Type':'application/x-www-form-urlencoded; charset=UTF-8','X-Requested-With':'XMLHttpRequest','Accept':'application/json, text/javascript, */*; q=0.01'},body:new URLSearchParams({XNXQDM:term}).toString()});console.log('[ClassTrack 课表导出] 响应状态',response.status,response.statusText);if(!response.ok){throw new Error('HTTP '+response.status+' '+response.statusText)}const text=await response.text();let data;try{data=JSON.parse(text)}catch(error){throw new Error('响应不是有效的 JSON')}const rows=data?.datas?.cxxszhxqkb?.rows;if(!Array.isArray(rows)||rows.length===0){throw new Error('未获取到课表数据')}console.log('[ClassTrack 课表导出] 原始响应',data);console.table(rows.map(({KCM,SKJS,SKXQ_DISPLAY,KSJC_DISPLAY,JSJC_DISPLAY,JASMC,YPSJDD})=>({课程:KCM,教师:SKJS,星期:SKXQ_DISPLAY,开始节:KSJC_DISPLAY,结束节:JSJC_DISPLAY,教室:JASMC,安排:YPSJDD})));window.__classTrackCourseData=data;const filename='classtrack-course-'+term.replace(/[^0-9a-zA-Z_-]/g,'-')+'.json';const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json;charset=utf-8'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=filename;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);alert('课表 JSON 已导出：'+filename)}catch(error){console.error('[ClassTrack 课表导出] 请求失败',error);alert('课表导出失败，请确认已登录教务系统且在正确页面执行，然后重试。')}})()`
}
