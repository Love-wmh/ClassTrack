.DS_Store
.env
.env.*
*.log

/node_modules/
/coverage/
/dist/
/.vite/

# React Router
/.react-router/
/build/

# IDE files
.idea/
.vscode/

# Capacitor / Android 构建产物
# 工程源码在 android/，具体忽略规则见 android/.gitignore
*.apk
*.aab
*.ap_
android/.gradle/
android/local.properties
android/app/build/
android/build/
android/captures/
android/app/src/main/assets/public
android/capacitor-cordova-android-plugins/

# iOS 尚未接入，预留生成目录
ios/

# 执行环境 bind mount 的 /dev/null（字符设备），不是项目文件
/.bash_profile
/.bashrc
/.gitconfig
/.mcp.json
/.profile
/.ripgreprc
/.zprofile
/.zshrc

# 容器内 pnpm 跨文件系统时自动落在工作区的下载缓存（纯缓存，可再生）
/.pnpm-store/

# 发布签名密钥库与凭据：绝不入库（本机副本放 ~/.classtrack-secrets/）
*.jks
*.keystore
