node_modules
dist
build
.vite
.idea
.vscode
.react-router
coverage
app/components/ui
*.md
**/*.md

# Capacitor / Android 原生工程与生成资源
android
ios
*.apk
*.aab

# 各 Agent 平台的配置目录与 Trellis 生成物
# 这些内容由 trellis init / 各 CLI 生成并维护，重新格式化只会与上游产物产生无意义 diff
.agents
.claude
.codebuddy
.codex
.pi
.trellis

# 说明：根目录那几个由执行环境 bind mount 的 /dev/null 设备文件（.bashrc / .mcp.json 等）
# 不需要在这里重复列出。prettier 的 --ignore-path 默认值就是 [.gitignore, .prettierignore]，
# 它们已由 .gitignore 覆盖；实测只保留 .gitignore 一处时 pnpm format:check 即为退出码 0。
# 注意上方的 .pi / .trellis 等必须留在这里：那些目录已被 git 跟踪，.gitignore 管不到。
