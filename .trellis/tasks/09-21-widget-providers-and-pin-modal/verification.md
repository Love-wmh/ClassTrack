# 验收记录：多 provider + 快速失败探针 + 醒目标态框

> 任务 [prd.md](./prd.md) / [design.md](./design.md) / [implement.md](./implement.md)
> 实施方式：主线程直接改（用户明确要求不使用子代理）。
> 本文件在实施过程中逐项填写证据；真机项（A6）以 PKR110 / ColorOS / Android 16 为准。

## 结论

（待填）

## A1 模态状态机与文案有测试 ⏳

（待填：vitest 用例名 + 断言要点，含「requesting 文案绝不含『已添加』」「失败态含按尺寸手动步骤」）

## A2 快探针有 JUnit 覆盖 ⏳

（待填：`PinAttemptTest` 的全部用例与结果）

## A3 五个 provider 注册与跨层标签正确 ⏳

（待填：`WidgetProvidersTest` + 读真实资源文件的断言 + 资产脚本对 APK 内两份 info XML 的断言）

## A4 放置即带样式 ⏳

（待填：provider → 默认样式映射的单测；五档各放一个实例的日志与截图；
2×2 极简、2×3 / 4×2 单栏、4×3 看宽度、6×3 双栏）

## A5 多 provider 下配置页与 pin 链路正确 ⏳

（待填：配置页归属校验接受五个 provider；pin 基线/回调集合跨 provider 并集且去重）

## A7 真机复验 ⏳

| 项 | 预期 | 实测 |
|---|---|---|
| (a) 点击即弹模态 | 点击后立即出现「正在尝试添加…」 | 待填（截图 + 时间戳） |
| (b) 失败立即切态 | 探针命中后立即出现失败态（不等满 10 秒） | 待填（与 (a) 的时间差） |
| (c) 拾取器五个条目 | `课表 · 极简 2×2` / `课表 · 手机 2×3` / `课表 · 宽横 4×2` / `课表 · 标准 4×3` / `课表 · 宽屏 6×3`，尺寸标签分别 2×2 / 2×3 / 4×2 / 4×3 / 6×3 | 待填（截图） |
| (d) 放下即带样式 | 各实例样式与其 provider 的预览一致（2×2 极简、2×3 / 4×2 单栏、4×3 看宽度、6×3 双栏） | 待填（`layout_metrics` 日志 + 截图） |
| (e) 拖动改尺寸自动匹配 | 4×3 ↔ 6×3 拖动后单/双栏与行项形态跟着变；配置页显式选样式后不再自动变 | 待填（日志 + 截图） |

## A6 尺寸变化自动匹配（R4）⏳

（待填：`WidgetPreset.match(w,h)` 最近邻的用例与边界结果；「显式样式优先」不变式的测试名；
真机拖动 4×3 ↔ 6×3 后单/双栏与行项形态自动变化、以及在配置页显式选样式后不再自动变）

## A8 门禁 ⏳

（待填：`testDebugUnitTest` / `pnpm test` / `pnpm lint` / `cap:build:android` 结果与用例数）

## 残余风险与待办

（待填）

## 过程缺陷（踩到就记）

（待填）
